# ハタチュウ Links

シンプルな静的リンクページです。ブラウザで `index.html` を開くだけで表示できます。

## 更新する場所

紹介文やリンクを変えるときは `content.js` を編集します。

- `intro`: 紹介文の行
- `profileLinks`: ハッシュタグやクレジットのリンク
- `sections`: SNS、配信、その他のリンク一覧

画像を差し替える場合は `assets/avatar.webp` と `assets/stand.webp` を同じファイル名で置き換えます。
